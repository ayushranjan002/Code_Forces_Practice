#include <bits/stdc++.h>
using namespace std;

int main() {
    int a, b, c;
    cin >> a >> b >> c;

    vector<int> v = {a, b, c};
    sort(v.begin(), v.end());
    int median = v[1];

    int ans = abs(median - a) + abs(median - b) + abs(median - c);
    cout << ans;

    return 0;
}
